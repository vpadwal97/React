name: Check Workspace Docs

on:
  push:
    branches:
      - main  # Adjust branch name as needed

jobs:
  check_workspace_docs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      
